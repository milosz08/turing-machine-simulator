import * as ReactDOM from 'react-dom/client';
import { ReduxStoreWrapperComponent } from '@/router/redux-store-wrapper.component';

ReactDOM.createRoot(document.getElementById('app-mount')).render(<ReduxStoreWrapperComponent />);
