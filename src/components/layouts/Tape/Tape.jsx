import React from 'react';

import { TapeContainer } from './Tape.styles';

const Tape = () => {
    return (
        <TapeContainer>
            Tape Container
        </TapeContainer>
    );
};

export default Tape;